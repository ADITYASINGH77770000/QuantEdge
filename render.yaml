services:
  - type: web
    name: quantedge-streamlit
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m streamlit run app/main.py --server.port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
